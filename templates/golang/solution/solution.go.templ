package solution

import (
	"fmt"
	"io"
	"math"
)

type Input io.Reader
type Answer int

func SolveP1(input Input) (Answer, error) {
	return 0, nil
}

func SolveP2(input Input) (Answer, error) {
	return 0, nil
}
